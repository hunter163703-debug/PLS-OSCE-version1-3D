# PLS 版本製作指南（版本 2 / 版本 3）

> 本專案已將所有設定集中化。製作新版本時，**大部分設定只需修改一個檔案**：
> `js/VersionConfig.js`
>
> 最後更新：2026-08-30｜現行版本：版本一（20260830l）

---

## 一、快速流程總覽

```
1. 複製整個專案資料夾  →  OPENCODE3D互動網站-v2
2. 更換題目圖卡圖片    →  測驗題本圖片檔/ 資料夾內同名檔案
3. 修改題目邏輯        →  js/InteractionLogic.js（見第三節）
4. 修改版本設定        →  js/VersionConfig.js（見第二節）
5. 修改頁面標題        →  index.html 的 <title> 與 <h1>
6. 本機測試 → GitHub Pages 部署（見第五節）
```

---

## 二、js/VersionConfig.js 設定詳解

### 2.1 版本識別
| 設定 | 說明 |
|---|---|
| `id` | 版本代號（'v1' / 'v2' / 'v3'）|
| `title` | 顯示用名稱（'PLS OSCE 版本二'）|

※ 另需手動修改 index.html 的 `<title>` 與 `<h1>`（加入版本名稱）

### 2.2 標準病人
| 設定 | 說明 |
|---|---|
| `character.name` | 人物名稱（狀態列＋字幕自動套用，目前「小宇」）|
| `character.modelPath` | 3D 模型路徑（Mixamo 標準骨架 FBX）|
| `character.voiceFile` | 備援語音檔 |
| `character.scaleHeight` | 模型正規化高度（公尺，預設 1.55）|

※ 若更換人物：新模型必須是 **Mixamo 標準骨架（mixamorig: 前綴）**，動作檔才相容。

### 2.3 題目圖卡
| 設定 | 說明 |
|---|---|
| `cards` | 圖卡按鈕 → 圖片檔對應（封面/圖卡一~五）|
| `cardItems` | 各圖卡上「指認項目」的 UV 座標 |

※ 更換圖卡圖片後，**必須用 `calibrate.html` 重新校準 UV 座標**，
  更新 `cardItems`，否則「指答案」的手指圖示會指錯位置。

### 2.4 測驗結構
| 設定 | 說明 |
|---|---|
| `exam.phaseTotals` | 各階段總題數（測驗結束判定）|
| `exam.storyKeywords` | 故事理解關鍵詞（朗讀故事偵測）|
| `exam.totalSec` | 施測總時數（秒）|
| `exam.warnSec` | 剩餘時間提醒（秒）|

### 2.5 場景與注視（通常不需改）
| 設定 | 說明 |
|---|---|
| `scene.waveWall` | 波浪軟墊背景牆尺寸/位置 |
| `scene.lights` | 燈光強度（角色亮度）|
| `gaze.pitchDeg` | 視線上仰角度（預設 15°）|

---

## 三、題目修改位置導覽（js/InteractionLogic.js）

> 題目邏輯集中在 `handleUtterance()` 方法（約 141 行起），
> 每題是一段 `if(關鍵詞判斷){ 動作+字幕+setPhase }`。

### 題目區段地圖（依行號，供快速定位）

| 區段 | 內容 | 說明 |
|---|---|---|
| ~141 | `handleUtterance()` 開頭 | 語音進入點 |
| ~151 | 故事理解問答攔截 | 考生朗讀後的問句優先處理 |
| ~160 | 故事朗讀偵測 | 圖卡二/三 → `_handleStory()` |
| ~174 | 第 1 題 | 複合指令（點頭）|
| ~180 | 練習題 | 拍拍手／摸摸頭／握握手／點點頭 |
| ~230 | 語言理解 | 各圖卡一題目（手套、飛機、顏色、條件、食物追問…）|
| ~480 | 故事理解題 | 第一次聽（沉默）→ 第二次（作答）|
| ~530 | 圖卡四口語表達 | 第 16、17 題（揹書包、看大象）|
| ~600 | 輔助方法 | `_doClap()`、`_doNod()`、`_answerStoryQuestion()` 等 |

### 修改一題的模板
```js
// 第 X 題：<題目說明>
if(t.includes('<考生會說的關鍵詞>')){
  this.setPatient('（<顯示給監考者的動作說明>）');
  this.setCaption('<字幕：人物反應>', 3000);
  this.mm.play('<動作key>', {once:true});        // 動作key見 animFiles
  this.setPhase('<階段名稱>','<題目key>');         // 標記完成
  return;
}
```

### 修改時的三條鐵律
1. **`setPhase(階段, 題目key)`** 的題目 key 必須與 index.html 的
   `data-q="..."` 完全一致，右側進度樹才會正確點亮。
2. **題數**：`VersionConfig.exam.phaseTotals` 的數字必須與實際題數一致，
   否則測驗不會結束（或提前結束）。
3. **指認題**要同步更新 `VersionConfig.cardItems` 的 UV 座標
   （用 calibrate.html 校準）。

---

## 四、語音替換

| 內容 | 位置 |
|---|---|
| 人物「說話」的語音 | `小安腳本語音(口語回應使用)/` 資料夾 |
| 關鍵詞 → MP3 對應表 | `js/VoicePlayer.js` 開頭 `VOICE_SCRIPTS` |
| 無對應時的備援 | `VersionConfig.character.voiceFile` |

新版本的語音檔放同資料夾、在 VOICE_SCRIPTS 加對應即可（各版本共用無妨）。

---

## 五、測試與部署

```bash
# 本機測試（專案資料夾內）
python -m http.server 8000
# 瀏覽器 http://localhost:8000（Ctrl+F5 強制刷新）
```

部署到 GitHub Pages（每個版本建議獨立 repo 或獨立資料夾路徑）：
```bash
git add -A ; git commit -m "PLS 版本二" ; git push origin main
```
※ GitHub Pages 部署需 1~3 分鐘，測試前 Ctrl+F5。

---

## 六、版本驗收檢查清單

- [ ] 待機：坐著（併腿）、雙手放腿上、抬頭注視考生、有呼吸起伏
- [ ] 右欄全部題目可見（含最後兩題）
- [ ] 練習題 3 題：拍手／摸頭／握手／點頭 動作正確
- [ ] 指認題：手指圖示與 3D 紅點指對位置（UV 校準）
- [ ] 故事題：第一次朗讀沉默、第二次才作答
- [ ] 口語表達：對應語音 MP3 播放
- [ ] 第 17 題結束 → 測驗結束提示遮罩
- [ ] 計時器 10 分鐘、8 分鐘提醒
- [ ] 重置腳本狀態：一切歸零
