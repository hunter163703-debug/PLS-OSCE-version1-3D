# 3D OSCE 臨床考試模擬演練網站 — 進度筆記與工作日誌

> 專案路徑：`C:\Users\Administrator\Desktop\見習與實習\技能考\OPENCODE3D互動網站`
> 最後更新：2026-07-06

---

## 一、專案目標

建構「修訂學前兒童語言障礙評量表 PLS OSCE 版本一」的 3D 臨床考試模擬演練平台，讓考生能對 3D 標準病人「小安」進行語音互動，並依腳本觸發對應動作與口語回應。

---

## 二、已完成功能

### 2.1 核心模組

| 模組 | 檔案 | 說明 |
|------|------|------|
| 場景建構 | `js/SceneSetup.js` | Three.js 場景、相機、燈光、圖卡平面、指認標記 |
| 模型載入 | `js/ModelLoader.js` | FBX 載入、AnimationMixer、Crossfade 平滑過渡 |
| 互動邏輯 | `js/InteractionLogic.js` | 語音狀態機、圖卡互動、故事延遲、進度追蹤 |
| 語音輸出 | `js/VoicePlayer.js` | Web Speech API zh-TW TTS |
| 主程式 | `js/main.js` | 模組組裝、語音辨識、UI 事件、渲染迴圈 |
| 樣式 | `css/style.css` | 16pt 高對比 UI、一屏無捲動 |
| 校準工具 | `calibrate.html` | 圖卡 UV 座標點擊校準 |

### 2.2 互動腳本

- 練習題：拍拍手、摸摸頭、握握手、點點頭
- 圖卡一語言理解：手套指認、食物全部指出來（分心 → 蘋果 → 追問 → 牛奶 → 香蕉）
- 圖卡四口語表達：「揹書包，妹妹揹」；無效追問時搖頭不語
- 故事理解：第一次聽完等待 3 秒不回應，第二次才作答
- 複合指令：先點頭再閉眼（序列播放）

### 2.3 UI 狀態

- 右側顯示：麥克風狀態、考生輸入語音（即時顯示 interim + final）、圖卡控制、測驗進度
- 測驗進度：分測驗二練習題（練習題一、二、三）、分測驗二語言理解（題目一~十五）、分測驗二口語表達（題目一~五）、測驗四（題目一、二）

---

## 三、已知問題與修正記錄

| 日期 | 問題 | 原因 | 修正 |
|------|------|------|------|
| 2026-07-03 | 模型載入失敗：obj is not defined | `const obj` 只在 try 區塊內有效，try 外 `if(obj)` 造成 ReferenceError | 刪除 try 外的 `if(obj)` |
| 2026-07-03 | 圖卡/模型 URL 含中文與全形冒號導致載入失敗 | Three.js loader 未對中文路徑編碼 | 在 `loadFBX` 與 `setCard` 中加入 `encodeURI()` |
| 2026-07-03 | 3D 場景全黑，人物、圖卡、除錯球皆不可見 | 移除桌子後，`SceneSetup.js` return 仍引用 `tableTop` 變數，導致整個 `createSceneSetup()` 拋錯 | 從 return 物件中移除 `tableTop` |
| 2026-07-03 | 畫面非一屏、右側出現捲動 | 響應式斷點在 ≤860px 上下堆疊 | 強制並排顯示，隱藏捲動條 |
| 2026-07-03 | 圖卡最下方說明欄需刪除 | 圖卡圖片本身含底部文字 | 載入紋理時以 `tex.repeat/offset` 裁切下方 12% |
| 2026-07-05 | 所有 3D 動畫無法執行 | Three.js r160 中 `AnimationInterpolants` 已不存在，`ModelLoader.js` 設定該屬性導致動畫註冊拋錯 | 刪除 `action.setInterpolation(...)` 行 |
| 2026-07-05 | 整個網站空白（人物、圖卡、麥克風皆失效） | `InteractionLogic.js` 第 125 行多餘 `)` 導致 SyntaxError，整個 ES Module 無法載入 | 刪除多餘的 `)` |
| 2026-07-05 | 麥克風無法關閉 | `Speech.listening` 是閉包內部變數，未暴露給外部 | 加入 `get listening(){ return listening; }` getter |
| 2026-07-05 | 相機設定導致人物被裁切或特寫 | 初始相機位置與 `main.js` 載入模型後的動態覆蓋不一致 | 統一在 `main.js` 依 bounding box 動態計算相機位置 |
| 2026-07-05 | 語音輸出偶有 `interrupted` 錯誤 | 快速連續觸發 `speak()` 時前一句被取消 | 加入 `synth.resume()` 與 fallback 到 MP3 |

---

## 四、已完成

### 2026-08-29

1. [x] 標準病人更換：小安 → 小宇（`小宇人物模型/rig.fbx`，Mixamo 標準 65 骨骨架 + 完整蒙皮），11 個動作 FBX 骨名完全相容、動作 100% 保留、動作檔零修改
2. [x] 全站文字「小安」→「小宇」（語音檔名與資料夾路徑保持不變）
3. [x] 坐姿鎖定（`ModelLoader.js` 新增 `_applySittingBase()`）：取樣「坐著」clip 為基準，將所有單次動作的 Hips 位置軌道與雙腿旋轉鎖定為坐姿常數，修正 Mixamo 站立版動作把角色拉站起來、超出構圖的問題——握手、點頭、摸頭、指圖卡等全部改為坐著完成
4. [x] 摸頭動作重建：原始「摸頭.fbx」僅 2 幀（壞檔），以「坐著下半身 + 原始摸頭上半身姿勢」組合重製為 60 幀完整動作（抬手摸頭 → 輕拍兩下 → 放下）
5. [x] 圖卡區視覺調整：移除黃色分隔線，背景改為暗木色漸層（模擬桌面質感）；圖卡位置、手指圖示、指答案邏輯零變動
6. [x] 3D 桌子評估：Table.fbx 經視錐驗證於現有半身構圖下不可見且會遮擋握手手部，依「不影響既有功能」原則決定不導入（檔案保留於小宇人物模型/）
7. [x] 版本號強制更新機制（?v=20260829b）：解決瀏覽器快取舊版 JS 導致修正未生效的問題
8. [x] .gitignore 精簡：排除 zip/GLB/原始模型備份（約省 60MB），僅保留網站必要模型（rig.fbx、摸頭.fbx 等）

### 2026-07-05

1. [x] 確認 3D 人物與圖卡顯示正常
2. [x] 調整相機與構圖：人物呈現胸口以上，圖卡以 HTML overlay 顯示於下方
3. [x] 右側面板擴大為畫面 1/3，測驗進度更新為正確題數（練習題3題、語言理解15題、口語表達5題、測驗四2題）
4. [x] 整合麥克風啟動/停止為單一 Toggle 按鈕
5. [x] 新增「手指頭圖示」動畫：人物指向圖卡時，overlay 上出現 👆 並播放按壓動畫
6. [x] 測驗結束後彈出提示：「測驗尚未完成，後方還有題目，請完成紙本記錄紙第四頁，黑框內的題目。」
7. [x] 完善 InteractionLogic.js 狀態機：依據進度筆記腳本，對應動作回應、口語回應、指圖卡
8. [x] 修復 `ModelLoader.js` `AnimationInterpolants` 不存在錯誤（Three.js r160）
9. [x] 修復 `InteractionLogic.js` 語法錯誤（多餘 `)` 導致整個模組崩潰）
10. [x] 相機自動對準：依據模型 bounding box 動態調整相機位置
11. [x] 放寬語音辨識匹配條件（故事題、食物題、圖卡四口語表達）
12. [x] 刪除圖卡控制中的「封面」與「圖卡五」按鈕
13. [x] 初始顯示圖卡一（而非封面）
14. [x] 圖卡 overlay 背景改為黑色

### 2026-07-06

15. [x] 人物構圖最終修正：`css/style.css` 讓 canvas 佔 stage 上 60%，避免被 overlay 遮蓋；相機改為斜上方俯視，聚焦上半身，確保臉部/胸口完整入鏡
16. [x] 根據 Markdown 腳本（`將此 OSCE 腳本更改為 Markdown 格式產出給我.md`）補齊全數 17 題：
    - 分測驗二語言理解 第1~15題（圖卡一：複合指令、手套、飛機、位置、顏色、條件、食物追問等）
    - 故事理解 5 題（圖卡二/三：聆聽兩次後問答）
    - 分測驗二口語表達 第16~17題（圖卡四：揹書包追問、從頭到尾複述）
17. [x] `VoicePlayer.js` 改為佇列機制（queue）：前一句 `onend` 才播下一句，徹底解決 `interrupted` 搶話中斷
18. [x] 新增對話雲（Speech Bubble）：人物說話時右上角顯示白色氣泡框，內含文字
19. [x] `SceneSetup.js` 修復指認標記座標：marker 從 `cardMesh` 子物件改為獨立 `scene` 物件，使用 `localToWorld()` 轉換世界座標 + `lookAt(camera)` 面向相機
20. [x] `InteractionLogic.js` 加入 `_busy` 忙錄鎖：防止 `handleUtterance` 被語音辨識重入干擾
21. [x] 修復 `Speech.onResult` async 回呼導致的 Chrome 拋錯：`main.js` 恢復為同步回呼，移除 `async/await` 外層包裝，避免 `message channel closed before a response was received`
22. [x] 更新所有 cache busting 版本號（統一為 `?v=20250705o`）

---

## 五、待處理事項

1. [ ] **指圖卡標記精確度**：目前 marker 位置為目測估計 UV，需使用 `calibrate.html` 工具點擊校正，確保蘋果、香蕉、牛奶、杯子、手錶、報紙、手套位置 100% 準確
2. [ ] **語音 TTS 無聲音問題**：若 Windows/Chrome 未安裝中文語音套件，TTS 會靜音（雖已自動退回 MP3，但 MP3 只有 4.7 秒固定音檔）。未來需接 ElevenLabs/Azure 等外部 API 做真正音色克隆
3. [ ] **補製缺失動畫**：`搖頭.fbx`、`閉眼睛.fbx`（已有 `分鏡參考/` 圖片，放進 `模擬病人：3D 模擬人物小安/` 即可自動生效）
4. [ ] **逐題實機測試驗證**：語言理解 1~15 題、故事理解 5 題、口語表達 16~17 題，需在實際 Chrome/Edge 環境中用麥克風逐一測試，確認動畫+語音+指圖卡+對話雲同步無誤
5. [x] **Git 版本控制**：已初始化並發布至 GitHub（2026-08-03，見 Round「GitHub 上架與發布」）

---

## 五、資源清單

### 5.1 3D 模型與動畫

路徑：`模擬病人：3D 模擬人物小安/`

- `小安.fbx` — 主模型
- `坐著.fbx` — 坐姿 idle
- `拍拍手.fbx` — 拍手
- `點點頭.fbx` — 點頭
- `是.fbx` — 贊同
- `握手.fbx` — 握手
- `摸頭.fbx` — 摸頭
- `指.fbx` — 指物
- `說話.fbx` — 說話
- `分心-站.fbx` — 分心
- `打招呼.fbx` — 打招呼

### 5.2 圖卡素材

路徑：`測驗題本圖片檔/`

- `封面.JPG`
- `圖卡一.png`
- `圖卡二.png`
- `圖卡三.png`
- `圖卡四.JPG`
- `圖卡五.JPG`

### 5.3 語音

- `小安.MP3` — 4.7 秒，目前作為 Web Speech 無 zh-TW 語音時的備援播放

### 5.4 腳本檔案

- `修訂學前兒童語言障礙評量表PLS  OSCE版本一腳本.pdf`
- `修訂學前兒童語言障礙評量表PLS  OSCE版本一腳本.docx`

---

## 六、執行方式

```powershell
cd "C:\Users\Administrator\Desktop\見習與實習\技能考\OPENCODE3D互動網站"
python -m http.server 8000
```

以 Chrome/Edge 開啟 `http://127.0.0.1:8000/index.html`。

---

## 七、設計決策摘要

- 使用原生 Three.js + ES Module importmap，避免建構工具依賴
- 語音辨識使用瀏覽器內建 Web Speech API（zh-TW）
- 語音輸出使用瀏覽器內建 SpeechSynthesis，以較高 pitch 模擬兒童音色
- 高對比、16pt 字級、無裝飾元素，符合臨床教學無障礙需求
- 一屏無捲動，確保考試過程不被 UI 干擾

---

## 八、分鏡參考圖

已於 `分鏡參考/` 資料夾生成：
- `閉眼睛_分鏡參考_*.png`
- `搖頭_分鏡參考_*.png`

供未來補製 `閉眼睛.fbx`、`搖頭.fbx` 動畫時參考。

---

## 六、今日工作摘要

### 2026-07-05（第一天）

#### 上午
- 開工同步：讀取進度筆記、檢查 git status（非 git repo）
- 調整相機構圖、移除除錯球、清理 console.log
- 啟動本地伺服器測試，發現模型載入錯誤

#### 下午
- 發現並修復 `AnimationInterpolants.InterpolateLinear` 不存在錯誤
- 發現並修復 `InteractionLogic.js` 多餘 `)` 語法錯誤（導致整個網站空白）
- 修復麥克風 Toggle 無法關閉問題
- 調整 layout：stage 分上下（3D人物+圖卡overlay）、右側面板擴大為 1/3
- 新增手指頭圖示動畫、測驗結束提示遮罩
- 更新測驗進度題數（練習題3、語言理解15、口語表達5、測驗四2）
- 放寬語音辨識匹配條件、整合麥克風按鈕、刪除多餘圖卡按鈕
- 人物位置持續微調中（目前只顯示帽子，需進一步調整）

### 2026-07-06（第二天）

#### 上午
- 人物構圖最終修正：CSS canvas 佔 stage 上 60%，相機改為斜上方俯視，聚焦上半身，確保臉部/胸口完整入鏡
- 根據 Markdown 腳本補齊全數 17 題（語言理解 1~15、故事理解、口語表達 16~17）
- `VoicePlayer.js` 改為佇列機制，徹底解決 `interrupted` 搶話中斷
- 新增對話雲（Speech Bubble）UI：人物說話時右上角顯示白色氣泡框

#### 下午
- 修復指圖卡標記座標：marker 從 `cardMesh` 子物件改為獨立 `scene` 物件，使用 `localToWorld()` + `lookAt(camera)`
- `InteractionLogic.js` 加入 `_busy` 忙錄鎖防止 `handleUtterance` 被語音辨識重入干擾
- 修復 `Speech.onResult` async 回呼導致的 Chrome 拋錯（`message channel closed before a response was received`）
- 更新所有 cache busting 版本號為 `?v=20250705o`

### 2026-07-06（下午更新）

23. [x] 重新讀取更新後的 Markdown 腳本（使用者提供新語音與搖頭動畫、刪除閉眼動畫）
24. [x] `ModelLoader.js`：新增 `搖頭.fbx` 動畫註冊，移除閉眼動畫相關邏輯
25. [x] `InteractionLogic.js`：
    - 第1題改為只執行「點頭」動作（閉眼動畫與腳本皆已刪除）
    - 故事理解：第一次聽完等待 3 秒後表現「分心」動作（符合新腳本）
    - 更新 `STORY_KEYWORDS` 為新故事關鍵詞（動物園、小明、媽媽、火車站、爸爸）
    - `_doCompoundNodCloseEyes()` 改為僅點頭並加上相容註解
26. [x] `VoicePlayer.js`：重構為「腳本 MP3 優先」機制，建立 `SCRIPT_VOICES` 映射表，特定句子自動播放對應錄音檔（媽媽、看大象、坐車車、爸爸、揹書包、妹妹揹、早上的時候、在家裡、鞋子、沒有、起床了妹妹穿衣服刷牙揹書包），第二次「看大象」自動切換為 `看大象2.mp3`
27. [x] 更新所有 cache busting 版本號為 `?v=20250706a`

### 2026-07-06（傍晚修復）

28. [x] **修復動畫播放問題**：`ModelLoader.js` 重構 `_crossfadeTo`，改用 Three.js 原生 `fadeIn()` / `fadeOut()` 取代手動 RAF timer，解決 `effectiveWeight` 被 mixer update 覆蓋導致非拍手動畫無法顯示的問題
29. [x] **修復 finger icon 定位**：`InteractionLogic.js` 的 `_pointToItem` / `_pointToItems` 現在會將 UV 座標傳給 `onPointStart`；`main.js` 的 `showFingerTap` 接受 UV 並動態設定 CSS `left`/`bottom`；`style.css` 更新 `.finger-icon` 基礎定位與動畫 keyframes，使 👆 正確出現在圖卡對應物品位置
30. [x] **修復語音未使用音檔問題**：`VoicePlayer.js` 在 constructor 中預載所有腳本 MP3（`canplaythrough` / `error` 監聽），擴大標點正規化範圍，加入詳細 debug log（匹配嘗試 → 正規化 → 成功/失敗），確保腳本固定回應優先播放真實錄音
31. [x] 更新所有 cache busting 版本號為 `?v=20250706b`

### 2026-07-06（晚間修復 - 實機測試回饋）

32. [x] **修復 finger icon 精確定位**：`main.js` 的 `showFingerTap` 改為基於 `#cardImg` 實際渲染矩形計算像素座標，並考慮 Three.js 紋理裁切（offset.y=0.12, repeat.y=0.88）將 UV 轉回完整圖片座標；`style.css` 更新 `.finger-icon` 基礎定位為 `left:0; top:0; transform:translate(-50%,-50%)`
33. [x] **修復指多個物品重複動畫**：`InteractionLogic.js` 的 `_pointToItems` 重構為「只播放一次 point 動畫，動畫結束後依序移動 marker + finger icon」，消除每個物品間回 sit 的問題
34. [x] **放寬口語表達匹配**：圖卡四邏輯新增 `hasBackpack` 輔助變數，同時匹配「揹書包」「背書包」「背包」「背著書包」「揹著書包」；放寬「什麼時候」「哪裡」「東西」等關鍵詞；fallback 「事情」改為 `(t.includes('做') && t.includes('什麼'))` 更穩健
35. [x] **放寬故事理解匹配 + 移除嗯嗯 fallback**：`_answerStoryQuestion` 各問題匹配條件更寬鬆（容忍語音辨識差異）；未匹配時不再說「嗯嗯。」，改為播放點頭動畫（靜默），避免干擾測驗
36. [x] **修復題目15「帶在手上」匹配**：條件從 `t.includes('戴')` 擴展為 `t.includes('戴') || t.includes('帶') || t.includes('穿')`
37. [x] **修正口語表達題數**：`PHASE_TOTALS` 與 `index.html` 進度樹從 5 題改為 2 題（對應腳本第16、17題）
38. [x] 更新所有 cache busting 版本號為 `?v=20250706c`

### 2026-07-06（深夜修復 - 故事理解優先級）

39. [x] **修復故事理解被圖卡四搶答**：將 `storyHeardCount >= 2` 的問答檢查移至 `handleUtterance` **最頂層**，優先於所有圖卡邏輯。無論考生目前切換到哪張圖卡，只要已經聽過故事兩次，任何問句（誰/什麼/哪裡/怎麼/去哪/時候等）都會優先進入故事問答，不再被 圖卡四「在家裡」等條件攔截
40. [x] **擴大故事問答觸發詞**：新增 `哪裡`、`地方`、`時候` 等關鍵詞，降低語音辨識差異導致的漏匹配
41. [x] 更新所有 cache busting 版本號為 `?v=20250706d`，並在 `main.js` 加入 `console.log('[Main] 版本：20250706d')` 以便於 Console 確認是否載入最新版

### 2026-07-07（凌晨修復 - 實機回饋 Round 3）

42. [x] **校正圖卡一 UV 座標**：蘋果從 `[0.180, 0.820]` → `[0.260, 0.820]`（偏右修正）；杯子從 `[0.820, 0.500]` → `[0.750, 0.500]`（偏左修正）
43. [x] **延長 finger icon 停留時間**：`main.js` `showFingerTap` setTimeout 從 1400ms → 1500ms；`InteractionLogic.js` `_pointToItems` stayMs 預設從 900ms → 1500ms，確保每個物品至少停留 1 秒
44. [x] **修復圖卡四「他在做什麼」匹配失敗**：新增 `hasWhatDo` 輔助變數，容忍「做什麼/做什/做甚/做啥/幹嘛/幹什/幹嗎」及異體字；搖頭條件改為必須明確包含「事情」（避免與第一問衝突）
45. [x] **修復喇叭回音導致重複觸發**：`VoicePlayer.js` 新增 `onSpeakStart` / `onSpeakEnd` 回呼；`main.js` 設為播放小安語音前自動 `Speech.stop()`，結束後自動 `Speech.start()`，徹底消除喇叭輸出被麥克風收音的 Loop
46. [x] **加入故事理解未朗讀提示**：若考生在圖卡二/三問問題但 `storyHeardCount < 2`，顯示提示「請先朗讀故事兩次，小安才能回答問題」
47. [x] 更新所有 cache busting 版本號為 `?v=20250706e`

### 2026-07-07（上午修復 - 實機回饋 Round 4）

48. [x] **再次校正圖卡一 UV 座標**：蘋果從 `[0.260, 0.820]` → `[0.330, 0.820]`；杯子從 `[0.750, 0.500]` → `[0.670, 0.500]`
49. [x] **故事理解改為聽一次即可**：`storyHeardCount >= 2` 改為 `>= 1`，未朗讀提示同步調整
50. [x] **刪除圖卡三按鈕**：`index.html` 圖卡控制區移除「圖卡三」按鈕
51. [x] **圖卡四第16題 finger icon**：在 `CARD_ITEMS['圖卡四']` 新增「背書包」項目（UV `[0.500, 0.150]`，對應第4張圖片）；第16題匹配成功時顯示 👆 指向該位置
52. [x] **放寬第17題匹配**：`說說` 條件擴展為 `說說 || 說`，解決「請你從頭到尾再把故事說一次」未匹配問題
53. [x] 更新所有 cache busting 版本號為 `?v=20250706f`

### 2026-07-07（上午修復 - UV 座標微調）

54. [x] **第三次校正圖卡一 UV 座標**：
    - 蘋果：`[0.330, 0.820]` → `[0.450, 0.860]`（向右+向上，修正左下偏移）
    - 杯子：`[0.670, 0.500]` → `[0.820, 0.420]`（向右+向下，修正左上偏移）
55. [x] 更新所有 cache busting 版本號為 `?v=20250706g`

### 2026-07-07（上午修復 - UV 座標第四次微調）

56. [x] **第四次校正圖卡一 UV 座標**：
    - 蘋果：`[0.450, 0.860]` → `[0.520, 0.900]`（再向右+再向上，修正仍偏左下）
    - 杯子：`[0.820, 0.420]` → `[0.750, 0.350]`（再向左+再向下，修正仍偏右上）
57. [x] 更新所有 cache busting 版本號為 `?v=20250706h`

#### 目前狀態
- 動畫系統正常運作（clap、nod、point、greet、distract、**shakeHead** 等皆可播放）
- 人物構圖已確認正常（臉部與胸口完整入鏡）
- 對話雲 UI 已完成，語音佇列機制已上線
- 全數 17 題腳本已根據**更新後的 Markdown 腳本**調整完畢
- **腳本語音 MP3 已整合**：11 句固定口語回應優先播放真實錄音，其餘回退 TTS/備援 MP3
- 指圖卡 marker 座標已修復為世界座標 + 面向相機，待使用 `calibrate.html` 精確校正

### 2026-08-03（實機測試修復 Round 5）

58. [x] **修復題目 5/6/8 無法觸發**：根因為題目 4 匹配條件過寬（`蘋果|香蕉|牛奶` + `哪|在|指`），攔截了含相同關鍵詞的 5/6/8 題。將條件具體的 5/6/7/8/10 移至題目 4 之前判斷；預防性將題目 13 移至題目 2 之前（「手套和報紙中間」含「手套+指」會被題目 2 攔截）。新順序：13→2→3→5→6→7→8→10→4→9→11→12→14→15
59. [x] **座標系統統一為完整圖片座標**：`CARD_ITEMS` 改為完整圖片 UV（左下 0,0 → 右上 1,1）；紋理裁切（offset.y=0.12, repeat.y=0.88）換算移至 `SceneSetup.showPointMarker` 內部處理（`vPlane=(v-0.12)/0.88`）；`main.js showFingerTap` 移除舊換算直接映射
60. [x] **校準工具升級**：`calibrate.html` 改為支援圖卡一 7 物品 + 圖卡四，表格點選物品→點擊圖片中心，可重複修正，輸出完整 `CARD_ITEMS` JS
61. [x] **圖卡一座標實機校準完成**：蘋果 `[0.230, 0.821]`、手套 `[0.500, 0.819]`、香蕉 `[0.766, 0.816]`、牛奶 `[0.500, 0.488]`、報紙 `[0.503, 0.160]`、杯子 `[0.759, 0.177]`、手錶 `[0.239, 0.177]`；圖卡四「背書包」仍為估計值 `[0.500, 0.252]` 待補校
62. [x] 更新所有 cache busting 版本號為 `?v=20260803b`

### 2026-08-03（實機測試修復 Round 6）

63. [x] **第12題移除口語回應**：`_handleYellowQuestion` / `_handleYellowFollowUp` 不再 `speak` / `showBubble`，只指牛奶、香蕉
64. [x] **第14題改為全程分心**：`_handleFoodAll` 不再主動指蘋果與說話，改為持續播放分心動畫（loop）；考生追問「還有呢？」後 `_handleFoodFollowUp` 才依序指出蘋果、香蕉、牛奶（不說話）；再追問則搖頭
65. [x] **修復分測驗四 16/17 題無法正確回應**：根因為故事理解優先層（`storyHeardCount>=1` + 問句關鍵詞）在考生進入圖卡四後仍攔截「做什麼/誰/哪裡」等問句。故事優先層加入 `currentCard !== '圖卡四'` 排除條件
66. [x] **新增第28題（故事理解答問）**：確認「分測驗二：語言理解（故事理解答問）」即為第28題；進度樹語言理解改為 16 題並於題目十五後新增「題目二十八（故事理解）」；`_handleStory` 朗讀故事時標記 `語言理解題目二十八`；`PHASE_TOTALS` 語言理解 15→16
67. [x] **刪除進度樹「測驗四（2題）」**：`index.html` 移除該 phase 區塊；`PHASE_TOTALS` 與 `_getUnimplementedQuestions` 同步移除
68. [x] **結束提示改版**：文字改為「施測尚未結束，請接續完成紙本題目當中黑色框框的部分，共計有 12 個位子要填寫，請仔細確認！」；`VoicePlayer` 新增 `busy` getter，`main.js showExamEnd` 改為輪詢等待語音佇列播完（第17題「起床了，妹妹穿衣服，刷牙，揹書包。」說完）才顯示遮罩
69. [x] 更新所有 cache busting 版本號為 `?v=20260803c`

### 2026-08-03（實機測試修復 Round 7）

70. [x] **第14題腳本再修正**：小安先分心約 5 秒（distract loop + setTimeout 5s）→ 指出蘋果 → 考生追問「還有呢？」→ 指出香蕉和牛奶；再追問則搖頭。全程不說話；`reset()` 會清除 `_foodTimer`
71. [x] **第28題問題(4)修復**：「誰送他們去火車站？」原被問題(1)「誰…去玩」條件攔截誤答「媽媽」。`_answerStoryQuestion` 重排判斷順序：Q4（誰+火車站/送/開車→爸爸）→ Q1（誰+玩→媽媽）→ Q5（怎麼→看大象）→ Q2（哪裡→看大象）→ Q3（坐/車→坐車車）
72. [x] **起始畫面改為封面**：`reset()` currentCard 預設改為「封面」；`main.js` 初始載入封面（overlay + 3D 圖卡平面）；考生於第2題手動切圖卡一、分測驗四手動切圖卡四；重置按鈕同步切回封面
73. [x] **結束提示觸發改為第17題完成即彈出**：新增 `_finishExam()`，第17題回答後直接觸發（仍等語音播完才顯示遮罩）；提示文字「位子」改「位置」
74. [x] **新增測驗計時器**：頂欄左側標題下方顯示 `mm:ss / 10:00`；考生第一次口語輸入（`Speech.onResult`）啟動；8 分鐘顯示閃爍「還剩 2 分鐘」（黃底）；10 分鐘顯示閃爍「施測時間已到，請離場」（紅底）並停止計時；重置按鈕歸零
75. [x] **CSS 佈局調整**：body 改 flex column、`#layout` 改 `flex:1;min-height:0`，頂欄高度隨計時器列自適應；新增 `#timerBox` / `.timer-flag` / `timerBlink` 閃爍動畫；`style.css` 連結補上 cache busting 版本號
76. [x] 更新所有 cache busting 版本號為 `?v=20260803d`
77. [x] **全物品座標二次校準完成**：圖卡一 7 物品微調（蘋果 `[0.226, 0.810]`、手套 `[0.501, 0.833]`、香蕉 `[0.763, 0.794]`、牛奶 `[0.491, 0.488]`、報紙 `[0.501, 0.158]`、杯子 `[0.755, 0.166]`、手錶 `[0.229, 0.183]`）；圖卡四「背書包」校準為 `[0.665, 0.194]`
78. [x] 更新所有 cache busting 版本號為 `?v=20260803e`

### 2026-08-03（實機測試修復 Round 8）

79. [x] **麥克風機制重構（意圖導向）**：`Speech` 模組改為三旗標設計——`desiredOn`（考生手動開關意圖）、`paused`（小安說話暫停）、`active`（引擎實際運行）。新增 `pauseAuto()` / `resumeAuto()`：小安說話僅暫停且**保留按鈕狀態與考生意圖**，說完保證自動恢復收音；修復舊實作的競態問題（`onSpeakEnd` 時引擎未完全關閉導致 `rec.start()` 拋錯、麥克風永久停擺）；暫停期間狀態列顯示「小安說話中（暫停收音）」
80. [x] **第17題觸發收緊**：必須明確聽到「**小美**」才觸發第17題回應與結束提示，避免第16題追問（「再說一次」等語句）誤觸發導致畫面直接跳到終止頁面
81. [x] 更新所有 cache busting 版本號為 `?v=20260803f`
82. [x] **結束提示延遲 2 秒**：第17題語音播完後再停頓 2 秒才顯示結束遮罩，讓考生有時間記錄；`examEndScheduled` 旗標防止重複排程，重置按鈕會一併歸零並隱藏遮罩
83. [x] 更新所有 cache busting 版本號為 `?v=20260803g`
84. [x] **圖卡四「背書包」三次校準**：UV 修正為 `[0.663, 0.073]`；圖卡一 7 物品同步微調
85. [x] 更新所有 cache busting 版本號為 `?v=20260803h`
86. [x] **修復麥克風靜音 4~5 秒自動停止**：根因為 Chrome 語音辨識在無語音約 5 秒後丟出 `no-speech` 錯誤事件，舊 `onerror` 將所有錯誤視為致命並關閉麥克風。修正為僅 `not-allowed`/`service-not-allowed`/`audio-capture` 嚴重錯誤才關閉；`no-speech`/`aborted` 等暫時性事件交由 `onend` 自動重啟（重啟加 150ms 延遲提高成功率）
87. [x] 更新所有 cache busting 版本號為 `?v=20260803i`

### 2026-08-03（GitHub 上架與發布）

88. [x] **全域安裝 connect-github 技能**：`~/.config/opencode/skills/connect-github/SKILL.md`（改編自 codex-lazy-packs 02-連接-GitHub.md v0.3）
89. [x] **Git 版本控制初始化**：`git init -b main`；`.gitignore` 排除 `測試影片1.mp4`、`Thumbs.db`、`desktop.ini`（解除待辦 #5）
90. [x] **發布到 GitHub**：repo `hunter163703-debug/PLS-OSCE-version1-3D`（public）；GitHub Pages 已啟用（main / root，legacy build，HTTPS enforced）
91. [x] **線上網址驗證 HTTP 200**：https://hunter163703-debug.github.io/PLS-OSCE-version1-3D/
